@inherits BootstrapComponentBase

@{
    string collapseId = $"{this.Id}-collapse";
}
<nav @attributes="this.Attributes">

    @if (!this.CollapseBrand)
    {
        @if (null != this.BrandTemplate)
        {
            @this.BrandTemplate(new Link { Url = this.BrandUrl, Text = this.BrandText })
        }
        else
        {
            <NavbarBrand Text="@this.BrandText" Url="@this.BrandUrl" ImageUrl="@this.BrandImageUrl" />
        }
    }

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#@collapseId" aria-controls="@collapseId" aria-expanded="false">
        <span class="navbar-toggler-icon"></span>
    </button>

    <NavbarCollapse Id="@collapseId">

        @if (this.CollapseBrand)
        {
            @if (null != this.BrandTemplate)
            {
                @this.BrandTemplate(new Link { Url = this.BrandUrl, Text = this.BrandText })
            }
            else
            {
                <NavbarBrand Text="@this.BrandText" Url="@this.BrandUrl" ImageUrl="@this.BrandImageUrl" />
            }
        }

        @this.ChildContent
    </NavbarCollapse>
</nav>
