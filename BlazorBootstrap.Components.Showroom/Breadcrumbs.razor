
<Heading Level="HeadingLevel.H2">Breadcrumbs</Heading>

@code {
    List<AnchorLink> items = new List<AnchorLink>()
    {
        new AnchorLink(){ LinkUrl="#", Text = "Home", Description="The front page of the site" },
        new AnchorLink(){ LinkUrl="#", Text = "Section 1" },
        new AnchorLink(){ LinkUrl="#", Text = "Section 1.1" },
        new AnchorLink(){ Text = "This page", IsActive = true, Description = "This is your current page. You are here now." }
    };
}

<Heading Level="HeadingLevel.H3" MarginTop="Spacing.Five">Simple</Heading>
<Breadcrumb Items="this.items" />

<Heading Level="HeadingLevel.H3" MarginTop="Spacing.Five">Custom Item Template</Heading>
<Breadcrumb Items="this.items">
    <ItemTemplate>
        @if (!string.IsNullOrEmpty(context.LinkUrl))
        {
            <Button Color="NamedColor.Light" Shadow="ShadowSize.Regular" Link="@context.LinkUrl" IsLink="true">@context.Text</Button>
        }
        else
        {
            <Span>@context.Text (@context.Description)</Span>
        }
    </ItemTemplate>
</Breadcrumb>