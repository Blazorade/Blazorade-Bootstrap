@inherits NavbarNavBase

<ul @attributes="this.Attributes">
    @if (null != this.ChildContent)
    {
        @this.ChildContent
    }
    else if (null != this.Items)
    {
        @foreach (var item in this.Items)
        {
            <NavItem Item="@item" />
        }
    }
</ul>
