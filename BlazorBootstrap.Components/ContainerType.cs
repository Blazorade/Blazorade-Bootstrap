using System;
using System.Collections.Generic;
using System.Text;

namespace BlazorBootstrap.Components
{
    public enum ContainerType
    {
        /// <summary>
        /// The container has a fixed max width at each breakpoint.
        /// </summary>
        FixedWidth,

        /// <summary>
        /// The container is fluid with a max width of 100%.
        /// </summary>
        Fluid

    }
}
