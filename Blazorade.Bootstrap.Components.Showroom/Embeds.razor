@code {
    string videoUrl = "https://www.youtube.com/embed/MetcuX1OHD0";
}

<Heading Level="HeadingLevel.H2">Embed Component</Heading>

<Paragraph>
    The <Anchor Url="https://getbootstrap.com/docs/4.5/utilities/embed/" OpenInNewTab="true">Embed component</Anchor> is used to create responsive video or slideshow embeds.
</Paragraph>

<DocsSection ComponentName="Embed"></DocsSection>


<Heading Id="basic" IsAnchor="true" Level="HeadingLevel.H3" MarginTop="Spacing.S5">Basic Embed</Heading>
<Paragraph>
    A basic <code>Embed</code> with a Youtube video with the default 16 to 9 aspect ratio.
</Paragraph>
<Embed Source="@videoUrl" />


<Heading Id="aspect-ratios" IsAnchor="true" Level="HeadingLevel.H3" MarginTop="Spacing.S5">Aspect Ratios</Heading>
<Paragraph>
    The <code>Embed</code> component also supports other aspect ratios than the default 16 to 9 which is shown above.
</Paragraph>

<CardGroup Type="CardGroupType.Deck">
    <Card Header="21 to 9">
        <Embed Source="@videoUrl" AspectRatio="EmbedAspectRatio.Ratio21by9" />
    </Card>
    <Card Header="4 to 3">
        <Embed Source="@videoUrl" AspectRatio="EmbedAspectRatio.Ratio4by3" />
    </Card>
    <Card Header="1 to 1">
        <Embed Source="@videoUrl" AspectRatio="EmbedAspectRatio.Ratio1by1" />
    </Card>
</CardGroup>
